package JarDeneme;
import com.elifsu.matematik.*; // paketler3 deki projeyi jar dosyası haline getirip ekledik
public class JarTest {
    public static void main(String[] args) {
        Matematik mat = new Matematik();

        mat.bolme(18,3);
        mat.carpma(16,5);
    }
}
